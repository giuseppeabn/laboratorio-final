name: DevSecOps Pipeline

on:
  push:
    branches:
      - devsecops-pipeline

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

      - name: Configurar Docker
        uses: docker/setup-buildx-action@v2

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        with:
          args: >
            -Dsonar.projectKey=giuseppeabn
            -Dsonar.organization=giuseppeabn
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}


      - name: Ejecutar SCA (Dependency Check)
        run: echo "Aquí va Dependency Check o similar"

      - name: Construir imagen Docker
        run: docker build -t movies-app .

      - name: Análisis de seguridad de imagen (Trivy)ß
        run: echo "Aquí va Trivy o similar"

      - name: Desplegar aplicación (Minikube o Docker local)
        run: echo "Aquí va el despliegue"

      - name: Análisis DAST (OWASP ZAP)
        run: echo "Aquí va ZAP o similar"
